`r if (knitr::is_html_output()) '
# 参考文献 {#references .unnumbered}
'`

```{r clean-book, cache=FALSE, include=FALSE}
# 移除 TikZ 图形产生的日志
file.remove(list.files(path = ".", pattern = "^tikz.*.log$"))
```

